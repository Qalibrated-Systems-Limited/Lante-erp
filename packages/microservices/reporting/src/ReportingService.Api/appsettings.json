{
  "ConnectionStrings": {
    "DefaultConnection": "${ConnectionStrings__DefaultConnection}",
    "AppConnection": "${ConnectionStrings__AppConnection}"
  },
  "JWT": {
    "SecretKey": "${JWT__SecretKey}",
    "Issuer": "LanteUserService",
    "Audience": "LanteUserService"
  },
  "TicketingService": {
    "BaseUrl": "${TicketingService__BaseUrl}"
  },
  "InternalServices": {
    "ServiceKey": "${InternalServices__ServiceKey}"
  },
  "AllowedHosts": "*",
  "OperationsService": {
    "BaseUrl": "${OperationsService__BaseUrl}"
  },
  "FleetService": {
    "BaseUrl": "${FleetService__BaseUrl}"
  },
  "StoreService": {
    "BaseUrl": "${StoreService__BaseUrl}"
  },
  "HseService": {
    "BaseUrl": "${HseService__BaseUrl}"
  },
  "ComplianceService": {
    "BaseUrl": "${ComplianceService__BaseUrl}"
  },
  "HrService": {
    "BaseUrl": "${HrService__BaseUrl}"
  },
  "FinanceService": {
    "BaseUrl": "${FinanceService__BaseUrl}"
  }
}
