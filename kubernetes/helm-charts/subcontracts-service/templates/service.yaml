apiVersion: v1
kind: Service
metadata:
  name: {{ include "subcontracts-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "subcontracts-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "subcontracts-service.selectorLabels" . | nindent 4 }}
