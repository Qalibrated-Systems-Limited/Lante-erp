apiVersion: v1
kind: Service
metadata:
  name: {{ include "hr-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hr-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hr-service.selectorLabels" . | nindent 4 }}
