apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gateway-service.fullname" . }}-yarp-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gateway-service.labels" . | nindent 4 }}
data:
  yarp.json: |
{{ .Files.Get "yarp.json" | indent 4 }}
