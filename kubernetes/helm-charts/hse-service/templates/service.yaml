apiVersion: v1
kind: Service
metadata:
  name: {{ include "hse-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hse-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hse-service.selectorLabels" . | nindent 4 }}
